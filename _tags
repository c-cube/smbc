<src/>: include
<src/**/*.*>: package(containers), package(msat), \
              package(containers.sexp), package(containers.data), \
              package(sequence)

true: safe_string, annot, bin_annot, color(always), optimize(3)
